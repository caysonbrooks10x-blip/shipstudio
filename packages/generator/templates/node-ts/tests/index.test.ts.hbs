import { describe, it, expect } from 'vitest';
import { create{{#each (splitCamelCase name)}}{{this}}{{/each}}, run } from '../src/index';

describe('{{name}}', () => {
  it('should create config', () => {
    const config = create{{#each (splitCamelCase name)}}{{this}}{{/each}}();
    expect(config.name).toBe('{{name}}');
  });

  it('should run', () => {
    expect(run()).toContain('{{name}}');
  });
});
